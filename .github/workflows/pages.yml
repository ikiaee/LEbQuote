name: Deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Pages
        uses: actions/configure-pages@v3
      
      - name: Prepare Files
        shell: cmd
        run: |
          mkdir docs 2>nul
          copy index.html docs\ >nul
          copy styles2.css docs\ >nul
          copy post_template2.html docs\ >nul
          echo Files in docs:
          dir docs
      
      - uses: actions/upload-pages-artifact@v2
        with:
          path: 'docs'
      
      - uses: actions/deploy-pages@v2
        with:
          path: 'docs'
      
      - uses: actions/deploy-pages@v2
